---
title: 订单模块
description: Order Module
icon: ShoppingCart
---

import { orderModule } from '@nomad/requirements/data';

## 订单模块

订单模块包含 {orderModule.requirements.length} 个功能需求，涵盖订单列表展示、状态标签筛选和搜索功能等核心功能。

### 模块概述

{orderModule.description}

### 需求列表

<div className="space-y-2 mb-8">
  {orderModule.requirements.map((req) => (
    <a
      key={req.id}
      href={`#${req.id}`}
      className="block p-3 border rounded-lg hover:bg-fd-secondary/50 transition-colors"
    >
      <div className="flex items-center justify-between">
        <span className="font-semibold">{req.id}: {req.name}</span>
        <span className={`text-xs px-2 py-1 rounded ${
          req.priority === 'Must Have' ? 'bg-red-100 text-red-800' :
          req.priority === 'Should Have' ? 'bg-yellow-100 text-yellow-800' :
          'bg-blue-100 text-blue-800'
        }`}>
          {req.priority}
        </span>
      </div>
    </a>
  ))}
</div>

---

## 详细需求

{orderModule.requirements.map((req) => (
  <RequirementDetail key={req.id} requirement={req} />
))}