/**
 * Validation schemas (Zod)
 *
 * Runtime validation schemas for forms, API inputs, and server actions
 * Used in validation layer: forms, API routes, server actions
 *
 * Type naming:
 * - Schemas: camelCase with "Schema" suffix (e.g., userInfoUpdateSchema)
 * - Types: PascalCase with "Data" suffix (e.g., UserInfoUpdateData)
 */

export * from "./auth";
